<!DOCTYPE html>
<html lang="en">

<head>
    <% include ../partials/_header %>
</head>

<body>
    <ul>
        <% for(var url in urls) { %>
            <li>URL:
                <%= urls[url] %>
            </li>
            <li> Shortened:
                <%= url %>
            </li>
            <li> Hyperlink:
                <a href="localhost:8080/u/<%= url %>">localhost:8080/u/<%= url %></a>
            </li>
            <li>
                <form method="POST" action="/urls/<%= url %>/delete"><label for="urlDelete">Delete URL:</label> <input type="submit" value="Submit"></form>
            </li>
            <li>
                <a href="localhost:8080/urls/<%= url %>">Edit</a>
            </li>
            <br>
            <% } %>
    </ul>
</body>

</html>