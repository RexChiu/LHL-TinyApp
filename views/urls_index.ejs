<!DOCTYPE html>
<html lang="en">

<head>
    <% include ../partials/_header %>
</head>

<body>
    <ul>
        <% for(var url in urls) { %>
            <li>Shortened URL:
                <%= url %>
            </li>
            <li>Full URL:
                <%= urls[url].longURL %>
            </li>
            <li>Number of Visitors:
                <%= urls[url].numVisited %>
            </li>
            <li>Date Created:
                <%= urls[url].dateCreated.toDateString() %>
            </li>
            <li>Hyperlink:
                <a href="localhost:8080/u/<%= url %>">localhost:8080/u/<%= url %></a>
            </li>
            <li>
                <form method="POST" action="/urls/<%= url %>/delete?_method=DELETE">
                    <label for="urlDelete">Delete URL:</label>
                    <input type="submit" value="Submit">
                </form>
            </li>
            <li>
                <a href="localhost:8080/urls/<%= url %>">Edit</a>
            </li>
            <li>
                <a href="localhost:8080/urls/new">Create a New Short Link</a>
            </li>
            <br>
            <% } %>
    </ul>
</body>

</html>