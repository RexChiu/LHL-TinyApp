<!DOCTYPE html>
<html lang="en">

<head>
    <% include ../partials/_header %>
</head>

<body>
    <ul>
        <% if (urls[shortURL].userID == cookie.user_id) { %>
            <li>Full URL:
                <%= urls[shortURL].longURL %>
            </li>
            <li>Shortened URL:
                <%= shortURL %>
            </li>
            <li>Number of Visitors:
                <%= urls[shortURL].numVisited %>
            </li>
            <li>Date Created:
                <%= urls[shortURL].dateCreated.toDateString() %>
            </li>
            <li>Unique Visitors:
                <%= Object.keys(urls[shortURL].uniqueVisits).length; %>
            </li>
            <li>Unique Visits:
                <% for (let key in urls[shortURL].uniqueVisits) { %>
                    <%= key %>: <%= urls[shortURL].uniqueVisits[key].toDateString() %>
                <% } %>
            </li>
            <form action="/urls/<%= shortURL %>?_method=PUT" method="POST">
                <li>Edit:
                <input id="longURL" type="text" name="longURL" placeholder="http://" style="width: 300px">
                <input type="submit" value="update">
            </form>
            </li>
            <% } else { %>
                Please login as URL's owner to view this URL's information
            <% } %>
    </ul>
</body>

</html>